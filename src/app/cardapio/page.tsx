import { metadataMenu } from "@/static/metadata";

export const metadata = {
  ...metadataMenu,
};

export default function Cardapio() {
  return (
    <main>
      <h1>Cardápio</h1>
    </main>
  );
}
