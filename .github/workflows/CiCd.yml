name: AlineBalasGourmet - CI/CD
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.16.x]
    steps:
      - name: Set NodeJs 🟢
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
      - run: yarn install
        name: Install Lib's 📦
        run: yarn type-check
        name: TypeCheck
      - run: yarn test::ci
        name: QA
